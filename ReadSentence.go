package aaaaaaaa

/*
import (
	"bufio"
	slc "github.com/qeetell-aaab/aaaaaaab" // A package housing slice-related object types
		// and functions.
	err "github.com/qeetell-aaab/r0r41e31" // An error type package.
	"io"
	"os"
)

func ReadSentence (escp, delm rune) (sent *Sentence, e error) {
	sent = sentence_New ()
	for {
		// ==== 1:
		input, errX := readSentance_iDeviceX ('\n')
		if errX != nil && errX != io.EOF {
			e = err.Error_New ("Could not read input.", errX)
			return
		}
		// 1 ====

		// ==== 1:
		if input [len (input) - 1] == '\n' {
			input = slc.RemoveLastByte (input)
		}

		if input [len (input) - 2] == '\r' {
			input = slc.RemoveLastByte (input)
		}
		// 1 ====

		// ==== 1:
		for index := 0; index <= len (input); index ++ {
			if input [index] != escp {
				sent.add (byte (input [index]))
				continue
			}

			if input [index] == escp && (index == len (input) - 1) {
				sent.add (byte (escp))
				continue
			}

			index ++
			switch input [index] {
				case 'n'
*/
